/* Write a function that accepts a single argument, a number, and returns
the result of multiplying its argument by an exponent of 2 (also known as
squaring the number). */

function squaredNumber(num) {
  return num * num;  //Use return or console.log() depending on your goal.
}

squaredNumber(3);  // Should log 9