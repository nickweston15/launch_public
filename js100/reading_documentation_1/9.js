// What will the following statements evalute to?

let tweet = "Starting to get the hang of it... #javascript #launchschool";

tweet.split(" "); // An array with each word as a separate value
tweet.split(" ").reverse(); // Same thing but with the words in reverse order
tweet.split(" ").reverse().join(" "); // A string with the words in reverse order