/* Locate the documentation for the Array built-in object on MDN. How can we
access the element "and" in the array ["fish", "and", "chips"]? */

// Well, you don't even need a method. Simply index the array.

console.log(["fish", "and", "chips"][1]);

let array = ["fish", "and", "chips"];
console.log(array[1]);