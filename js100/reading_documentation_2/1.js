// In the following snippet, find all violations of style guide and rewrite it.

let ice_cream_taste = 'chocolate' // let iceCreamTaste and use ;
let ice_cream_density = 10 // let iceCreamDensity and use ;

while(ice_cream_density > 0) // { should be here
{
    console.log('Drip...');
    ice_cream_density -= 1;
}

console.log('The '+ ice_cream_taste +' ice cream melted.'); // Use `` and ${} for var