// Find out how we can join two or more strings together. 

// You can use String.prototype.concat() which returns a new string.

let str1 = "Stint";
let str2 = "Hipwaist";

console.log(str1.concat(" ", str2));
console.log(str2.concat(", ", str1));