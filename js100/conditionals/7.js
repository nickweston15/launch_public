// Predict the output of the following code:

/* I think that it will only log "Yes!" because everything will either be
false OR true like the conditional sets. */

if (false || true) {
  console.log("Yes!");
} else {
  console.log("No...");
}