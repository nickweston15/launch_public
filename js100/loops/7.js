/* The following code keeps looping forever. You can hit CTRL + C to stop it.
Why does it loop forever? Modify it so that it stops after the first iteration. */

for (let i = 0; i < 1; i += 1) {     // Originally there was no condition to evaluate
  console.log("and on");
  // I put in a condition, but you also could put break; here
}