// What does the following code log to the console?

/* I know this one won't log anything because there is no console.log statement.
The return statement just stores the returned value, doesn't actually log it. */

function scream(words) {
  return words + "!!!!";
}

scream("Yipeee");